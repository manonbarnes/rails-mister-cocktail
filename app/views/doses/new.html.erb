<div class="container">
  <h1> Add a new dose!</h1>
  <%= simple_form_for [@cocktail, @dose] do |f| %>
    <%= f.input :description %>
    <%= f.association :ingredient, collection: @ingredients %>
    <br>
    <%= f.submit "Submit dose", class: "btn btn-flat-pink" %>
  <% end %>
</div>
